
    /**
     * file to record checked status when exit mock server
     * can be deleted if not needed
     * should ignored from git
     */
    module.exports = {
      mockChecked: {"www.example.com/api/login":1},
      setChecked: null
    }
  